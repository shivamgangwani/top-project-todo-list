import Project from "./modules/project";
import "./style.css"

const CONTENT = document.querySelector("div#content");


(function init() {
    // Create the skeleton of the page
    // 1. Sidebar with list of projects
    // 2. Remaining section to display todos
    CONTENT.textContent = "ABC";
})();